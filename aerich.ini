# Aerich Configuration for Database Migrations
#
# This file tells Aerich where to find your Tortoise ORM models
# and how to connect to the database.

from src.config import settings

TORTOISE_ORM = {
    "connections": {
        "default": settings.database_url
    },
    "apps": {
        "models": {
            "models": ["src.database.models", "aerich.models"],
            "default_connection": "default",
        }
    }
}
